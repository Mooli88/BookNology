import React from 'react';
import { IBookShelves } from '../../types/library';

interface Props extends IBookShelves {}

const Shelve = (props: Props) => {
	return <div>d</div>;
};

export default Shelve;
